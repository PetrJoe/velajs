# Users App

The `users` app demonstrates the default module layout:

- `controllers` handle HTTP concerns.
- `services` contain application workflows.
- `repositories` own SQL access.
- `validators` define Zod schemas.
- `ui` contains Tailwind-based components local to the app.

